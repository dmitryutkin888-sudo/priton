services:
  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "30385:8080"
      - "30386:8080"
    environment:
      PORT: 8080
    restart: unless-stopped
